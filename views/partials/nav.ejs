<%- include('./header') %>
<nav
  class="w-full px-4 pb-1 flex gap-10 justify-between items-center border-b-2"
>
  <section class="flex gap-10 text-xl font-semibold">
    <h1>Blog website.</h1>
  </section>
  <section class="flex gap-10 text-xl font-semibold">
    <a href="/">Home</a>
    <a href="/createpost">Ceate Blog</a>
    <a href="/">About</a>
  </section>
  <%if(user){%>
  <section class="flex gap-2 text-xl font-semibold">
    <a class="px-3 py-2 bg-zinc-400 rounded-sm" href="/profile">Profile</a>
    <a class="px-3 py-2 bg-zinc-400 rounded-sm" href="/users/logout">Logout</a>
  </section>

  <%}else{%>
  <section class="flex gap-2 text-xl font-semibold">
    <a class="px-3 py-2 bg-zinc-400 rounded-sm" href="/createuser">Register</a>
    <a class="px-3 py-2 bg-zinc-400 rounded-sm" href="/Login">login</a>
  </section>
  <%}%>
</nav>
<%- include('./footer') %>
